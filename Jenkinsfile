#!groovy
import groovy.json.JsonSlurper
import groovy.json.JsonOutput

pipeline {
    agent any


//    tools {nodejs "nodejs"}

    stages {
      stage('Input') {
        steps {
          Input(message: 'Informe o bucket:', ok: 'Submit')
          sh'''

          '''
        }
      }
    }
}